# 多字节 UTF-8 heading

Paragraph with escaped \*asterisk\*, `code`, {name}, and [duplicate](https://example.com/duplicate "duplicate").

Paragraph where duplicate appears as text before [duplicate](https://example.com/other "duplicate").

---

    indented_code("must stay untouched");

<!-- comment with {{not_a_runtime_placeholder}} -->

Final line without a trailing newline